## Packages
framer-motion | Page transitions and scroll-triggered animations

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  sans: ["var(--font-sans)"],
}
Colors should use the defined CSS variables for the Islamic theme (greens and golds).
